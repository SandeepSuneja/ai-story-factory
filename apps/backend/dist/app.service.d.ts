import { AssemblyAgent } from './agents/assembly.agent';
import { AudioAgent } from './agents/audio.agent';
import { CharacterAgent } from './agents/character.agent';
import { IdeaAgent } from './agents/idea.agent';
import { ImageAgent } from './agents/image.agent';
import { PromptAgent } from './agents/prompt.agent';
import { ScriptAgent } from './agents/script.agent';
import { VideoAgent } from './agents/video.agent';
import { StoryAgent } from './agents/story.agent';
import type { SceneScript } from './content-state';
import { GenerateCharacterProfileResponseDto, GenerateIdeaResponseDto, GenerateImageResponseDto, GeneratePromptResponseDto, GenerateScriptResponseDto, GenerateStoryResponseDto, GenerateVideoResponseDto, AssembleVideoResponseDto, GenerateAudioResponseDto } from './models/content.model';
export declare class AppService {
    private readonly ideaAgent;
    private readonly storyAgent;
    private readonly scriptAgent;
    private readonly characterAgent;
    private readonly promptAgent;
    private readonly imageAgent;
    private readonly videoAgent;
    private readonly audioAgent;
    private readonly assemblyAgent;
    constructor(ideaAgent: IdeaAgent, storyAgent: StoryAgent, scriptAgent: ScriptAgent, characterAgent: CharacterAgent, promptAgent: PromptAgent, imageAgent: ImageAgent, videoAgent: VideoAgent, audioAgent: AudioAgent, assemblyAgent: AssemblyAgent);
    getHello(): string;
    generateIdea(topic: string): Promise<GenerateIdeaResponseDto>;
    generateStory(idea: string): Promise<GenerateStoryResponseDto>;
    generateScript(story: string): Promise<GenerateScriptResponseDto>;
    generateCharacterProfile(story: string, script: SceneScript[]): Promise<GenerateCharacterProfileResponseDto>;
    generatePrompt(scene: SceneScript, characterAppearance: string): Promise<GeneratePromptResponseDto>;
    generateImage(scene: SceneScript): Promise<GenerateImageResponseDto>;
    generateVideo(scene: SceneScript): Promise<GenerateVideoResponseDto>;
    generateAudio(scene: SceneScript): Promise<GenerateAudioResponseDto>;
    assembleVideo(scenes: SceneScript[], projectName?: string): Promise<AssembleVideoResponseDto>;
}
